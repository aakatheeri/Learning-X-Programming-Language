#include <stdio.h>

int main() {

	// Pointers
	/* int foo = 24;
	int *bar = &foo; // That's mean bar is point to foo variable


	printf("%i\n", *bar); */

	// Count from 1 to 10
	/* for (int i = 1; i <= 10; i++) {
		printf("%i\n", i);
	} */

	// Count from 10 to 1
	for (int i = 10; i >= 1; i--) {
		printf("%i\n", i);
	}

	return 0;
}